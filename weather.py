import requests

class WeatherAgent:
    def get_weather(self, city, date):
        # For simplicity, using a mock weather API request
        # Replace this with a real API call (e.g., OpenWeather API)
        return "sunny"  # Mock response
